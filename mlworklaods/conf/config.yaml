
defaults:
  - _self_
  - dataloader: super #shade, torch_lru, super
  - dataset: imagenet
  # - training: ???
  - training: resnet18
  - override hydra/job_logging: disabled
  - override hydra/hydra_logging: disabled  

num_jobs: 4
num_devices_per_job: 1
seed: 41
accelerator: 'gpu'
log_dir: mlworklaods/reports
log_interval: 10
run_training: true
run_evaluation: false
dataload_only: false

# hydra:
#   mode: MULTIRUN
#   output_subdir: null
#   sweeper:
#     params:
#       training: resnet18
#       # training: resnet50, shufflenet, vgg16

